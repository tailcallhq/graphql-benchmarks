actions:
    - name: greet
      definition:
        kind: synchronous
        handler: '{{HASURA_GRAPHQL_ACTION_HANDLER_URL}}/greet'
    - name: posts
      definition:
        kind: synchronous
        handler: '{{HASURA_GRAPHQL_ACTION_HANDLER_URL}}/posts'
